# 🎮 Doubt Game - Sprint 1
## دليل التشغيل + استكشاف الأخطاء

---

## ⚡ التشغيل السريع

### المتطلبات
- Node.js v18+ (تحقق: `node --version`)
- npm (يأتي مع Node.js)

### الخطوة 1: تثبيت المكتبات
```bash
cd /d C:\Users\FAHAD\Downloads\Teacher\Doubt
npm install
```

### الخطوة 2: تشغيل Socket Server (Terminal 1)
```bash
cd /d C:\Users\FAHAD\Downloads\Teacher\Doubt
npm run server
```
**النتيجة المتوقعة:**
```
╔══════════════════════════════════════╗
║     🎮 Doubt Game Server v0.1       ║
║     Sprint 1: State Machine MVP      ║
║     Running on port 3001              ║
╚══════════════════════════════════════╝
Waiting for connections...
```

### الخطوة 3: تشغيل Next.js (Terminal 2)
```bash
cd /d C:\Users\FAHAD\Downloads\Teacher\Doubt
npm run dev
```
**النتيجة المتوقعة:**
```
▲ Next.js 14.2.21
- Local: http://localhost:3000
```

### الخطوة 4: اختبار في المتصفح
1. افتح http://localhost:3000
2. اكتب اسمك → اضغط "إنشاء جلسة جديدة"
3. افتح تاب ثاني → http://localhost:3000
4. اكتب اسم آخر → اضغط "الانضمام بكود" → أدخل الكود من التاب الأول
5. من تاب المضيف (الأول) → اضغط "ابدأ اللعبة"
6. شاهد الحالات تتغير تلقائياً

---

## ✅ تعريف النجاح (Acceptance Criteria)

| # | المعيار | الحالة |
|---|---------|--------|
| 1 | صفحة دخول: اسم + إنشاء/انضمام | ☐ |
| 2 | Lobby: قائمة لاعبين + كود جلسة | ☐ |
| 3 | زر "ابدأ" يعمل (للمضيف فقط) | ☐ |
| 4 | الحالات تدور تلقائياً | ☐ |
| 5 | كل اللاعبين يرون نفس الحالة | ☐ |

### الدورة المطلوبة:
```
LOBBY → NIGHT → MORNING → DISCUSSION → SILENCE → VOTING → RESULT → NIGHT...
        (10s)   (5s)      (15s)        (5s)      (10s)    (5s)
```

---

## 🧯 الأخطاء المتوقعة وحلولها

### A) `Cannot find module 'socket.io'` أو `ts-node not found`
**السبب:** npm install لم يتم أو فشل
**الحل:**
```bash
cd /d C:\Users\FAHAD\Downloads\Teacher\Doubt
npm install
```

### B) `EADDRINUSE: port 3001 already in use`
**السبب:** السيرفر مفتوح من قبل
**الحل (Windows):**
```bash
netstat -ano | findstr :3001
taskkill /PID [رقم_العملية] /F
```

### C) CORS Error / WebSocket فشل الاتصال
**السبب:** السيرفر مش شغال أو CORS مختلف
**الحل:** تأكد أن Terminal 1 يعرض السيرفر شغال على 3001 قبل فتح المتصفح

### D) Next.js لا يشتغل / خطأ في layout.tsx
**السبب:** هيكل الملفات غلط
**الحل:** تأكد من وجود:
```
src/app/layout.tsx   ← ضروري
src/app/page.tsx     ← ضروري
src/app/globals.css  ← ضروري
```

### E) الصفحة تفتح لكن Socket لا يتصل (شاشة "جاري الاتصال..." لا تختفي)
**السبب:** Socket server مش شغال
**الحل:** تأكد من:
1. Terminal 1 يعرض `Waiting for connections...`
2. افتح http://localhost:3001/health في المتصفح → يجب أن ترى `{"status":"ok"}`

### F) Hydration mismatch warnings
**السبب:** طبيعي في development مع RTL
**الحل:** تجاهلها في Sprint 1 (لا تأثير على اللعبة)

### G) `TypeError: Cannot read properties of null`
**السبب:** Socket لم يتصل بعد
**الحل:** الكود يتعامل مع هذا تلقائياً، إذا استمر → أعد تحميل الصفحة

---

## 📁 هيكل المشروع
```
Doubt/
├── package.json          ← التبعيات والأوامر
├── tsconfig.json         ← إعدادات TypeScript (Next.js)
├── tsconfig.server.json  ← إعدادات TypeScript (Socket Server)
├── next.config.js        ← إعدادات Next.js
├── tailwind.config.js    ← ثيم اللعبة
├── postcss.config.js     ← PostCSS
│
└── src/
    ├── app/              ← صفحات الواجهة (Next.js App Router)
    │   ├── globals.css
    │   ├── layout.tsx
    │   ├── page.tsx        ← شاشة الدخول
    │   ├── lobby/page.tsx  ← اللوبي + شاشة اللعبة
    │   └── game/page.tsx   ← placeholder
    │
    ├── server/           ← Socket Server (يعمل مستقل على 3001)
    │   ├── index.ts        ← نقطة التشغيل
    │   ├── socket.ts       ← أحداث Socket.IO
    │   └── gameEngine.ts   ← 🔥 محرك الحالات (State Machine)
    │
    ├── types/
    │   └── game.ts         ← كل الأنواع والثوابت
    │
    └── lib/
        ├── timer.ts        ← مؤقت الحالات
        └── useSocket.ts    ← React Hook للاتصال بالسيرفر
```

---

## 📋 أوامر مرجعية سريعة

| الأمر | الوظيفة |
|-------|---------|
| `npm install` | تثبيت المكتبات |
| `npm run server` | تشغيل Socket Server (3001) |
| `npm run dev` | تشغيل Next.js (3000) |
| `npm run dev:all` | تشغيل الاثنين معاً |
| `npm run build` | بناء للإنتاج |

---

## 🚫 ممنوع في Sprint 1
- لا أكشنات (محقق/مسعف/تصويت فعلي)
- لا تصميم متقدم
- لا قاعدة بيانات
- لا حسابات مستخدمين
- لا حماية متقدمة

## ✅ مطلوب تسليم (3 لقطات)
1. 📸 Terminal 1: السيرفر شغال على 3001
2. 📸 Terminal 2: Next.js شغال على 3000
3. 📸 المتصفح: الحالات تتغير تلقائياً لكل اللاعبين
